PIE Pudding
===========

A local amd64 pwn challenge about combining a format-string leak with a
position-independent executable and a stack overwrite.

Start the service:
  ./run.sh

Connect:
  nc 127.0.0.1 31337

Stop the service and remove its flag volume:
  ./run.sh stop

The author source and exploit are kept outside this distribution archive.
